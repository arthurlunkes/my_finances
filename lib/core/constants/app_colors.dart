import 'package:flutter/material.dart';

class AppColors {
  // Cores principais
  static const primary = Color(0xFF2E7D32); // Verde cristão
  static const primaryLight = Color(0xFF60AD5E);
  static const primaryDark = Color(0xFF005005);
  static const secondary = Color(0xFF1565C0); // Azul confiança
  static const secondaryLight = Color(0xFF5E92F3);
  static const secondaryDark = Color(0xFF003C8F);

  // Categorias de transação
  static const income = Color(0xFF4CAF50); // Verde - Receita
  static const expense = Color(0xFFE53935); // Vermelho - Despesa
  static const tithe = Color(0xFFFFB300); // Dourado - Dízimo
  static const offering = Color(0xFFFF6F00); // Laranja - Oferta
  static const creditCard = Color(0xFF5E35B1); // Roxo - Cartão
  static const savings = Color(0xFF00897B); // Teal - Economia

  // Status
  static const pending = Color(0xFFFF9800); // Laranja - Pendente
  static const paid = Color(0xFF66BB6A); // Verde - Pago
  static const overdue = Color(0xFFEF5350); // Vermelho - Atrasado

  // Neutros
  static const background = Color(0xFFF5F5F5);
  static const surface = Colors.white;
  static const surfaceDark = Color(0xFF121212);
  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const divider = Color(0xFFBDBDBD);

  // Feedback
  static const success = Color(0xFF4CAF50);
  static const error = Color(0xFFE53935);
  static const warning = Color(0xFFFF9800);
  static const info = Color(0xFF2196F3);
}
